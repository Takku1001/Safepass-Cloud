services:
  - type: web
    name: safepass-cloud
    runtime: node
    rootDir: web
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
